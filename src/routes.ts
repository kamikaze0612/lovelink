export const authRoutes = ["/login", "register"];

export const publicRoutes = ["/", "/about", "/contact", "/how-to-use"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_REDIRECT_AFTER_LOGIN = "/dashboard";
